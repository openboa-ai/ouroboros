interface:
  display_name: "PR CI Review Loop"
  short_description: "Drive PR CI, review, fix, and merge loops."
  default_prompt: "Use pr-ci-review-loop to publish the current branch, watch CI and review comments, apply bounded fixes, and merge only when clean."
