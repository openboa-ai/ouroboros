export interface ControlOption {
  id: string;
  title: string;
  scope: string;
  before: string;
  after: string;
  remains: string;
  authority: string;
  target: string;
  revision: string;
  domain: string;
}
